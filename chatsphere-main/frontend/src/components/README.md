Common components

- `Button` - rounded pill button using design system variables
- `IconButton` - circular icon button (default 40px)

Usage
import { Button, IconButton } from 'components/common';

Notes
- Use `var(--wa-*)` CSS variables for colors and spacing
- Keep these components stateless and accessible
